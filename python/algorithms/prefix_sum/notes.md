# 前缀和与差分（Prefix Sum / Difference Array）

## 简介

- **前缀和**：`S[i]=sum(a[0..i-1])`，区间和 `sum[l,r]=S[r+1]-S[l]`。
- **差分**：对区间 `[l,r]` 加 `v`：`d[l]+=v, d[r+1]-=v`，再前缀还原。

## 复杂度

建树 O(n)；查询/单点更新（简单版）O(1) 到 O(log n)（若用树状数组）。
