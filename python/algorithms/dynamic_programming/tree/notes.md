# 树形 DP（Tree DP）

## 简介

在树上 DFS 时合并子树信息：最大独立集、换根 DP 等。

## 复杂度

- 一次树 DFS + 子树信息合并：通常 **O(n)** 时间、**O(h)** 递归栈（`h` 为树高；链状树最坏 O(n)）。

## 边界

- **空树 / 单节点**：转移与返回值需有明确定义；`__main__` 中小样例覆盖。

## 示例

**打家劫舍 III**：树形版本，相邻不能同时选。
