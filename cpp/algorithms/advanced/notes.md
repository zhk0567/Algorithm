# 进阶专题（Advanced）

见 `sqrt_decomposition.cpp`，与 `sqrt_decomposition.py` 对应。

## 编译运行

```powershell
Set-Location cpp\algorithms\advanced
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe sqrt_decomposition.cpp
.\run.exe
```
