# 排序（Sorting）

实现见 `sorting.cpp`，与 `python/algorithms/sorting/sorting.py` 同构演示。

## 编译运行

```powershell
Set-Location cpp\algorithms\sorting
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe sorting.cpp
.\run.exe
```
