# 跳表（Skip List）

## 简介

多层有序链表，以概率提升层数；期望复杂度与平衡树同级，实现更简单。

## 操作与复杂度

| 操作 | 期望 |
|------|------|
| `search` / `insert` / `erase` | O(log n) |

## 典型应用

Redis `ZSet` 底层之一、有序集合替代。

## 代码要点

`p=0.5`，`MAX_LEVEL=16`；查找时从顶层向右、向下；插入记录 `update[]` 断点。
