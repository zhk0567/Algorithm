# LeetCode 23

见 `solution.cpp`：`priority_queue` 自定义比较。

## 编译运行

```powershell
Set-Location cpp\problems\leetcode\0023_merge_k_sorted_lists
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe solution.cpp
.\run.exe
```
