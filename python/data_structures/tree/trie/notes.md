# Trie（前缀树）

## 简介

多叉树，每条边表示一个字符；共享前缀，适合字典与前缀查询。

## 操作与复杂度

| 操作 | 时间 | 空间 |
|------|------|------|
| `insert` / `search` / `startsWith` | O(L) | O(AL) 节点 |

L 为字符串长度，A 为字母表大小（此处 26 个小写字母）。

## 典型应用

自动补全、词频统计、异或最大路径（0-1 Trie）。

## 代码要点

C++ 用 `array<Node*,26>` 或 `vector`；Python 用 `dict` 更省空间。
