# LRU 缓存

## 简介

容量固定，淘汰最久未使用项；`get`/`put` 均摊 O(1)。

## 核心思想

**哈希表**定位节点 + **双向链表**维护使用顺序（头最近、尾最久）。

## 操作与复杂度

| 操作 | 均摊 |
|------|------|
| `get` / `put` | O(1) |

## 典型应用

CPU 缓存、HTTP 缓存、数据库缓冲池。

## 代码要点

`get` 需把节点移到链表头；超容量删尾节点前从哈希表移除。
