# 栈（Stack）

## 简介

LIFO；数组或链表实现；最小栈 O(1) 取最小。

## 操作与复杂度

`push` / `pop` / `top`：O(1) 均摊。

## 编译运行

```powershell
Set-Location cpp\data_structures\linear\stack
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe stack.cpp
.\run.exe
```
