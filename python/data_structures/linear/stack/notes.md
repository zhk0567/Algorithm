# 栈（Stack）

## 简介

后进先出（LIFO）；可用动态数组或链表实现。最小栈在 O(1) 内维护当前最小值。

## 核心思想

- **数组栈**：`push/pop` 尾部，缓存友好。
- **辅助栈**：`MinStack` 同步压入当前最小值。

## 操作与复杂度

| 操作 | 时间 |
|------|------|
| `push` / `pop` / `top` | O(1) 均摊 |

## 典型应用

括号匹配、单调栈、DFS、表达式求值。

## 代码要点

链表栈注意头插与删除顺序；`MinStack` 与主栈同步弹出。
