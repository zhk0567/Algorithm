# LeetCode 15 — 3Sum（三数之和）

## 题面

- **来源**：LeetCode 15
- **链接**：<https://leetcode.cn/problems/3sum/>
- **简述**：找所有不重复三元组，使 `a+b+c=0`。

## 思路

排序 + 固定 `i`，双指针 `l,r`；跳过重复元素。

## 复杂度

O(n²) 时间，O(1) 额外空间（不计输出）。

## 陷阱与注意

- `i>0` 且 `nums[i]==nums[i-1]` 时跳过。
- 找到一组后 `l,r` 都要收缩并跳过重复。
