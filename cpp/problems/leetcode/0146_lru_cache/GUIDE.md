# LeetCode 146

见 `solution.cpp`：手写 `unordered_map<int, list<pair<int,int>>::iterator>` + `list`。

## 编译运行

```powershell
Set-Location cpp\problems\leetcode\0146_lru_cache
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe solution.cpp
.\run.exe
```
