# 邻接矩阵（Adjacency Matrix）

## 简介

`V×V` 矩阵，`M[u][v]` 表示边权；稠密图查询两点是否有边 O(1)。

## 操作与复杂度

| 操作 | 时间 | 空间 |
|------|------|------|
| 查边 (u,v) | O(1) | O(V²) |
| 遍历 u 邻居 | O(V) | |

## 典型应用

Floyd-Warshall、稠密图、小规模 V。

## 代码要点

无向图可只存上三角；无边用 0 或 `INF` 依题意。
