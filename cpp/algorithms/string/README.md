# 字符串算法

实现见 `string_algorithms.cpp`（与 Python 文件 `string_algorithms.py` 对应）。

## 编译运行

```powershell
Set-Location cpp\algorithms\string
g++ -std=c++17 -O2 -Wall -Wextra -o run.exe string_algorithms.cpp
.\run.exe
```
