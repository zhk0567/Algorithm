# 二叉堆（小根堆）

## 简介

完全二叉树用数组存储；`push`/`pop` 通过上浮、下沉维护堆性质。

## 操作与复杂度

| 操作 | 时间 |
|------|------|
| `push` / `pop` | O(log n) |
| `top` | O(1) |
| `heapify` | O(n) |

## 典型应用

Dijkstra、TopK、优先队列、堆排序。

## 代码要点

子节点 `2i+1, 2i+2`；下沉时选更小子节点交换。本实现 **不** 使用 `heapq`，手写数组堆。
